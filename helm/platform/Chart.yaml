apiVersion: v2
name: platform
description: Platform Helm chart for managing shared resources and app deployments
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: flask-app
    version: "0.1.0"
    repository: "file://../flask-app"
    condition: flask-app.enabled
